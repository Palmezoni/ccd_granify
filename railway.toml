[build]
builder = "nixpacks"
buildCommand = "pnpm run build"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && pnpm run seed && pnpm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
